# Local pre-change backups (see CLAUDE.md backup policy)
backup-*/

# Built release binary (attached to GitHub Releases, not tracked in git)
/hot-clone

# Local release tooling, not for the tracked repo
release.ps1
